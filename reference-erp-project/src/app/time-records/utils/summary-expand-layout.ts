/**
 * Expand chevron column cell styling on the summary table (narrow padding vs default `p-2`).
 * The nested detail rows table uses the same class on the first empty cell for alignment.
 */
export const SUMMARY_EXPAND_COLUMN_TD_CLASS = "w-7 min-w-7 max-w-7 p-1";
